# Fantasy reference textures

These are the supplied UI pieces kept as source references for the future Tui theme/media system.
They are not wired into the Azure Dark default theme in P1.

Planned use: panels, tabs, buttons, inputs, icon masks, list hovers, NineSlice borders and an optional Fantasy theme.
